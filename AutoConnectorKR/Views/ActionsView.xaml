<mah:MetroWindow x:Class="OLP.AutoConnectorKR.Views.ActionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OLP.AutoConnector.Views"
        xmlns:viewmodels="clr-namespace:OLP.AutoConnectorKR.ViewModels" 
        xmlns:resources="clr-namespace:OLP.AutoConnectorKR.Resources"
        
        d:DataContext="{d:DesignInstance Type=viewmodels:ActionsVM}"
                 
        mc:Ignorable="d"
        
        Title="Автосоединение элементов" Height="250" Width="400"
                 MaxHeight="{Binding WindowHeight}"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterScreen">

    <mah:MetroWindow.Resources>
        <ResourceDictionary>
            <FrameworkElement x:Key="ProxyElement" DataContext="{Binding}"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colors.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.AnimatedTabControl.xaml" />
                <ResourceDictionary Source="pack://application:,,,/AutoConnector;component/resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </mah:MetroWindow.Resources>
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!--Строка 0-->
        <TextBlock Grid.Row="0"
                   Style="{StaticResource TextBlockStyleDefault}"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Text="{Binding Instruction}"
                   Margin="10,0,10,0"
                   TextWrapping="Wrap"/>

        <!--Строка 1-->
        <StackPanel Grid.Row="1"
                    Orientation="Vertical"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Margin="10,0,0,0"
                    Visibility="{Binding AllowDisplayNextActions}">
            <RadioButton x:Name="radioButton1"
                         HorizontalAlignment="Left"
                         VerticalAlignment="Center"
                         Margin="5,5,5,5"
                         GroupName="NextActions"
                         Content="Выбрать закладные детали вручную на активном виде"
                         Checked="radioButton1_Checked"/>
            <RadioButton x:Name="radioButton2"
                         HorizontalAlignment="Left"
                         VerticalAlignment="Center"
                         Margin="5,5,5,5"
                         GroupName="NextActions"
                         Content="Выбрать все закладные детали видимые на активном виде"
                         Checked="radioButton2_Checked"/>
            <RadioButton x:Name="radioButton3"
                         HorizontalAlignment="Left"
                         VerticalAlignment="Center"
                         Margin="5,5,5,5"
                         GroupName="NextActions"
                         Content="Выбрать все закладные детали во всем проекте "
                         Checked="radioButton3_Checked"/>
        </StackPanel>
        <CheckBox Grid.Row="1"
                  x:Name="DoNotShowThisDialogCheckBox"
                  Style="{StaticResource CheckBoxStyleDefault}"
                  Content="Больше не показывать это окно"
                  Visibility="{Binding AllowDisplayDoNotShowThisWindowCheckBox}"/>

        <!--Строка 2-->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center"
                    Margin="0,0,10,0">
            <Button x:Name="OKButton"
                    Content="OK"
                    Style="{StaticResource ButtonStyleDefault}"
                    IsDefault = "true"
                    Click="OKButton_Click"/>

            <Button x:Name="CancelButton"
                    Content="Отмена"
                    Style="{StaticResource ButtonStyleDefault}"
                    IsCancel="True"
                    Visibility="{Binding AllowDisplayCancelButton}"
                    Click="CancelButton_Click"/>
        </StackPanel>

    </Grid>
    
    
</mah:MetroWindow>
