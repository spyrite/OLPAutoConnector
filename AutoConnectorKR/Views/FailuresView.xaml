<mah:MetroWindow x:Class="OLP.AutoConnectorKR.Views.FailuresView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OLP.AutoConnectorKR.Views"
        xmlns:viewmodels="clr-namespace:OLP.AutoConnectorKR.ViewModels" 
        xmlns:resources="clr-namespace:OLP.AutoConnectorKR.Resources"
        
        d:DataContext="{d:DesignInstance Type=viewmodels:FailuresVM}"
                 
        mc:Ignorable="d"
        
        Title="Автосоединение элементов: cписок ошибок" Height="400" Width="550"
                 ResizeMode="NoResize"
                 WindowStartupLocation="CenterScreen"
                 Topmost="True">

    <mah:MetroWindow.Resources>
        <ResourceDictionary>
            <FrameworkElement x:Key="ProxyElement" DataContext="{Binding}"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Colors.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/Blue.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Accents/BaseLight.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.AnimatedTabControl.xaml" />
                <ResourceDictionary Source="pack://application:,,,/AutoConnector;component/resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </mah:MetroWindow.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!--Строка 0-->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
            <TextBlock Text="Активный документ:"
                       Style="{StaticResource InfoTextBlockStyle}"/>
            <TextBlock Text="{Binding ProjectName, Mode=OneWay}"
                       Style="{StaticResource InfoTextBlockStyle}"
                       Foreground="{x:Static resources:Colors.TextBlockForegroundInfoColor}"
                       TextWrapping="Wrap"
                       Width="230"/>
            <CheckBox IsChecked ="{Binding Topmost, RelativeSource={RelativeSource AncestorType=Window}}" 
                      Content="Поверх всех окон" 
                      Margin="10,0,10,0"/>
        </StackPanel>

        <!--Строка 1-->

        <ListView Grid.Row="1"
                  ItemsSource="{Binding FailureModels}"
                  HorizontalContentAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  BorderBrush="LightGray"
                  BorderThickness="1">

            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource DGColumnHeaderStyle}">
                    <GridView.Columns>
                        <GridViewColumn Header="СООБЩЕНИЕ ОБ ОШИБКЕ"
                                        Width="300">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Path=Message}"
                                               Style="{StaticResource DGTextBlockStyle}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="КОЛ. ЭЛ-ТОВ"                                        
                                        Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Path=IdsCount}"
                                               Style="{StaticResource DGTextBlockStyle}"
                                               HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="ДЕЙСТВИЯ"
                                        Width="160">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                                HorizontalAlignment="Center"
                                                Margin="0,0,0,0">
                                        <Button x:Name="SelectIds"
                                                Style="{StaticResource DGButtonStyle}"
                                                Content="Выделить"
                                                Command="{Binding Path=SelectIdsCommand}"/>
                                        <Button x:Name="ShowIds"
                                                Style="{StaticResource DGButtonStyle}"
                                                Content="Список Id"
                                                Command="{Binding Path=ShowIdsCommand}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView.Columns>
                    
                </GridView>
            </ListView.View>
            
        </ListView>

        <!--Строка 2-->

        <Button Grid.Row="2"
                x:Name="CloseButton"
                Content="Закрыть"
                Style="{StaticResource ButtonStyleDefault}"
                HorizontalAlignment="Right"
                IsCancel="True"
                Margin="0,0,10,0">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <i:CallMethodAction MethodName="Close"
                                            TargetObject="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window }}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Button>


    </Grid>
</mah:MetroWindow>
